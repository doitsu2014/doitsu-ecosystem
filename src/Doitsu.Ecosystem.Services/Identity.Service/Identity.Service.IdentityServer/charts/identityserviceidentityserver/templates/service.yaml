apiVersion: v1
kind: Service
metadata:
  name: {{ template "identityserviceidentityserver.fullname" . }}
  labels:
    app: {{ template "identityserviceidentityserver.name" . }}
    chart: {{ template "identityserviceidentityserver.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "identityserviceidentityserver.name" . }}
    release: {{ .Release.Name }}
